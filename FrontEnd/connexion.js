const connexionButton = document.getElementsByClassName("buttonConnexion")
connexionButton.addEventListener("click", (event) => {
    event.preventDefault();
let emailForm = document.formLogin.email.value;
let passwordForm = document.formLogin.password.value;
}) 